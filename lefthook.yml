# LEFTHOOK - Configuration
# Referência: https://github.com/evilmartians/lefthook

pre-commit:
  parallel: true
  commands:
    # --------------------------------------------------------------------------
    # MOBILE (Flutter)
    # --------------------------------------------------------------------------
    mobile-lint:
      root: apps/mobile # Define o diretório de execução
      glob: "**/*.dart" # Só roda se arquivos .dart mudarem
      run: dart analyze .

    mobile-tests:
      root: apps/mobile
      glob: "**/*.dart"
      run: flutter test # Roda Unit e Widget tests

    # --------------------------------------------------------------------------
    # BACKEND (NestJS - Placeholder para seu stack)
    # --------------------------------------------------------------------------
    # backend-lint:
    #   root: apps/backend
    #   glob: "**/*.ts"
    #   run: npm run lint

# Pre-Push é o momento ideal para testes pesados (Integração)
pre-push:
  parallel: false
  commands:
    mobile-integration-tests:
      root: apps/mobile
      glob: "**/*.dart"
      # Atenção: Requer emulador rodando ou device conectado.
      # Pode falhar se não houver ambiente gráfico configurado.
      run: flutter test integration_test